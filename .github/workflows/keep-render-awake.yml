name: Keep Render awake

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping CafeFlow API health
        run: curl -fsS --max-time 90 https://cafeflow-hdmg.onrender.com/health
